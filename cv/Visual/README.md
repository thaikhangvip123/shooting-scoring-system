#Visual : Lấy tọa độ các phát bắn để hậu xử lí Heatmap, C50.... 
Hiện tại đang tự random phát đạn do chưa tích hợp bên detect vết đạn.

#Cài thư viện:
pip install opencv-python numpy

#Sửa lại đường dẫn ảnh cho đúng.